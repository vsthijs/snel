import x86 from "x86.snel";

module linux {
    /* linux common constants */

    /* linux syscalls */

    fn sys_read(fd: u32, buf: *u8, usize count) u32 {}
    
    fn sys_write(fd: u32, buf: *u8, usize count) u32 {}
    
    fn sys_open(filename: *u8, flags: u32, mode: u32) u32 {}
    
    fn sys_close(fd: Int) Int {}
}